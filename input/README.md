# Input Folder

Place the application inventory workbook here before running the orchestrator.

Expected file type: `.xlsx`

Set the path in `.env`:

```env
INPUT_FILE=input/your_inventory.xlsx
INPUT_SHEET=Sheet1
```

Do not commit real client workbooks.
